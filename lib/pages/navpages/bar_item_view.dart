

import 'package:flutter/material.dart';

class BarItemView extends StatelessWidget {
  const BarItemView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Bar View'));
  }
}